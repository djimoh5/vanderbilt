{
  "name": "vanderbilt-e2e-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Puppeteer-driven browser E2E scripts for manually verifying UI flows against a locally running dev server + backend. Not part of the app build.",
  "dependencies": {
    "puppeteer-core": "^24.0.0"
  }
}
